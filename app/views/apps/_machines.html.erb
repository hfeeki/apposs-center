            <input type="hidden" name="data" />
            <ul class="nav nav-list">
              <li class="nav-header">
                <table><tr>
                    <td>
                      服务器
                      <%=link_to ' ', machines_app_path(app.id), 
                        remote: true, class: 'icon-refresh', title: '刷新' %>
                    </td>
                    <td>
            <div refer_to="machines">
              <a href="javascript:void 0;" select="all">全选</a>
              <a href="javascript:void 0;" select="none">清除</a>
            </div>
                    </td>
                </tr></table>
              </li>
            </ul>
            <div style="height:200px; overflow-y:auto; overflow-x: hidden"> 
            <table class="table">
              <thead>
                <tr>
                </tr>
                </thead>
                <tbody id="machines">
                <% current_user.owned_machines(current_app).each{|machine|%>
                  <tr id="machines_<%=machine.id %>"> 
                    <%=render '/apps/machine_item', :machine => machine, :app => app %>
                  </tr>
              <% }%>
              </tbody>
            </table>
          </div>
           </ul>

